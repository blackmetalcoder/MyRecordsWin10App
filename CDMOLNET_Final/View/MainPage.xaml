<Page
    x:Class="CDMOLNET_Final.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDMOLNET_Final.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Text="Logga in" Style="{StaticResource TitleTextBlockStyle}" DataContext="{x:Bind Frame}" Visibility="{Binding PageTitleVisibility}" />
            <TextBlock Text="Användare" Style="{StaticResource CaptionTextBlockStyle}"></TextBlock>
            <TextBox x:Name="txtUser"></TextBox>
            <TextBlock Text="Lösenord" Style="{StaticResource CaptionTextBlockStyle}"></TextBlock>
            <PasswordBox x:Name="txtPassword"></PasswordBox>
            <Button x:Name="btnLoogain" Content="Logga in" Click="btnLoogain_Click"></Button>
            <TextBlock x:Name="txtInfo" Style="{StaticResource HeaderTextBlockStyle}"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
